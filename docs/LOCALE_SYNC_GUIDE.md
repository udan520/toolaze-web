# 多语言 SEO 内容同步指南

## 概述

本项目使用自动同步工具来管理多语言的 SEO 内容。**英语版本是源文件**，其他语言版本会自动从英语版本同步。

## 工作流程

### 1. 修改英语内容

编辑 `src/data/en/image-compression.json` 或 `src/data/en/image-converter.json`

**示例：添加新的 slug**
```json
{
  "new-tool-slug": {
    "metadata": {
      "title": "New Tool Title | Toolaze",
      "description": "Description of the new tool..."
    },
    "hero": {
      "h1": "New Tool Name",
      "sub": "Subtitle",
      "desc": "Tool description..."
    }
  }
}
```

### 2. 运行同步脚本

```bash
npm run sync-locales
```

这个脚本会：
- ✅ **自动检测**英语版本中的所有 slug
- ✅ **为每个语言创建/更新**对应的 JSON 文件
- ✅ **保留已翻译的内容**，不会覆盖你的翻译
- ✅ **添加新的 slug 或字段**，使用英语作为模板

### 3. 翻译内容

编辑各个语言目录下的 JSON 文件（如 `src/data/de/image-compression.json`），翻译其中的英语内容。

## 同步脚本的智能行为

### ✅ 保留已翻译内容

如果你已经翻译了某个 slug 的 `metadata.title`，运行同步脚本后：
- ✅ **已翻译的 `title`**：保持不变
- ✅ **新增的字段**：自动添加（使用英语模板）
- ✅ **新的 slug**：自动添加（使用英语模板）

### 示例场景

**场景 1：添加新 slug**

1. 在 `src/data/en/image-compression.json` 中添加 `new-tool`
2. 运行 `npm run sync-locales`
3. 所有语言文件自动添加 `new-tool`（使用英语内容）

**场景 2：修改现有 slug**

1. 在 `src/data/en/image-compression.json` 中修改 `png-to-100kb`，添加新字段 `features`
2. 运行 `npm run sync-locales`
3. 所有语言文件：
   - ✅ 保留已翻译的 `metadata.title`
   - ✅ 自动添加新字段 `features`（使用英语）

**场景 3：添加新字段到现有 slug**

1. 英语版本添加了 `sections.comparison`
2. 运行 `npm run sync-locales`
3. 所有语言版本自动添加 `sections.comparison`（使用英语模板）

## 目录结构

```
src/data/
├── en/                          # 英语源文件（主要编辑这个）
│   ├── image-compression.json
│   └── image-converter.json
├── de/                          # 德语（运行同步后自动生成）
├── ja/                          # 日语
├── es/                          # 西班牙语
├── zh-TW/                       # 繁体中文
├── pt/                          # 葡萄牙语
├── fr/                          # 法语
├── ko/                          # 韩语
└── it/                          # 意大利语
```

## 最佳实践

### 1. 工作流程

```
1. 编辑英语内容 (src/data/en/*.json)
   ↓
2. 运行同步脚本 (npm run sync-locales)
   ↓
3. 翻译新添加的内容（可选）
   ↓
4. 重复步骤 1-3
```

### 2. 翻译建议

- 📝 **逐步翻译**：不需要一次性翻译所有内容
- ✅ **同步会自动保留**你的翻译
- 🔄 **定期运行同步**：每次修改英语内容后运行一次

### 3. 内容修改

- ⚠️ **不要直接删除**其他语言文件中的 slug（即使英语版本删除了）
- ✅ **优先修改英语版本**，然后运行同步
- ✅ **直接翻译各语言文件**中的内容即可

## 命令行选项

### 基本用法

```bash
npm run sync-locales
```

### 输出示例

```
🌍 Starting multi-language SEO content sync...
   Supported locales: en, de, ja, es, zh-TW, pt, fr, ko, it
   Tools: image-compression, image-converter

📝 Processing image-compression.json
   Found 11 slugs in English version
   ✅ Created directory: de
   ✅ de: Added 11 new slug(s): jpg-to-20kb, png-to-100kb, ...

✨ Sync completed!

📋 Next steps:
   1. Check the generated files in src/data/[locale]/
   2. Translate the English content in each language file
   3. Run this script again after updating English content to sync new changes
   ℹ️  Note: Translated content will be preserved when syncing
```

## 故障排除

### 问题：同步后内容被覆盖

**原因**：可能手动修改了结构，导致合并逻辑出错

**解决**：
1. 检查 `src/data/en/` 中的源文件结构
2. 确保结构一致
3. 如果问题持续，可以从备份恢复翻译内容

### 问题：新字段没有同步

**原因**：可能是嵌套结构的字段

**解决**：
1. 确保英语版本的结构完整
2. 运行同步脚本
3. 检查生成的 JSON 文件

## 技术支持

如有问题，请检查：
1. `scripts/sync-locales.ts` - 同步脚本源代码
2. `src/data/README.md` - 数据结构说明
3. 运行日志 - 查看详细输出

# Seedance 2.5 Live Generator Implementation Plan

1. Add failing model-config, pricing, API-payload, page-rendering, history, and content-contract tests.
2. Register `seedance-2-5` in frontend and backend model registries.
3. Add optional multi-video and multi-audio reference state, validation, upload, payload, history display, and Recreate restoration.
4. Implement reference-video-aware pricing in browser and Cloudflare calculators.
5. Route the Seedance 2.5 landing page through `AiVideoGeneratorTool` and remove the launch-update branch.
6. Create the SEO Factory task and synchronize all nine locale content records with public data.
7. Run focused tests, generation-contract validation, rendered-page smoke checks, and visible-copy negative scans.
8. Start the worktree preview on port 3014 and verify `/model/seedance-2-5`.
